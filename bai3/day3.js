// console.log() => in  giá trị ra bảng console
console.log("abc");

// Kiểu dữ liệu trong javascript: chuỗi(string), số(number), true/flase(boolean), underfined(), null(), object()
// Cách để kiểu dữ liệu: typeof
// Chuỗi + số => 1 chuỗi mới
// Biến là đại diện cho 1 trường dữ liệu n đó và giúp ng đọc dễ hiểu hơn
// Khai báo biến trong js : let, var, const
// Công thức: let/var/const tên_biến = gtri n đó
// update gtri của biến
var address = "Ha Noi"
console.log('tôi đg ở' + address)

// camal case: viết hoa chữ cái đầu của từ thứ 2 trở đi
// snake case: các từ đc ngăn cách bởi dấu gạch dưới
// ${tenBien}: dùng để đọc biến trong chuỗi

// Hàm prompt trong js: cho phép ng dùng khai báo gtri từ trình duyệt
// Hàm alert("Lời dẫn") => Thông báo ra ngoài trình duyệt

// Phép tính cơ bản trong js: +, -, *, /, **
// Dấu căn bậc 2: Math.sqrt()